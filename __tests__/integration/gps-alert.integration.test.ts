describe.skip('gps-alert endpoints (skeleton — "initial" stub)', () => {
  it.todo('GET /api/gps/alerts lists unresolved alerts for the provider');
  it.todo('PATCH /api/gps/alerts/:id/resolve marks an alert resolved');
  it.todo('rejects resolving alerts that belong to a different provider');
});
