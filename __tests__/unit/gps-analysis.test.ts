describe.skip('gps-analysis job (skeleton — "initial" phase stub)', () => {
  it.todo('flags sessions with no movement beyond a threshold');
  it.todo('raises a gps_alert when provider leaves the service area geofence');
  it.todo('ignores sessions older than the retention window');
  it.todo('tolerates noisy GPS accuracy values');
});
